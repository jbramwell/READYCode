<Window x:Class="ReadyCode.Views.CodeStatisticsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        WindowStyle="ThreeDBorderWindow"
        AllowsTransparency="False"
        Background="Transparent"
        Width="340" SizeToContent="Height"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False"
        Title="Code Statistics">

    <Window.Resources>
        <Style x:Key="TitleBarCloseButton" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{DynamicResource ThemeSettingsBreadcrumbFg}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="bg" Background="{TemplateBinding Background}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="bg" Property="Background" Value="#E81123"/>
                                <Setter Property="Foreground" Value="#FFFFFF"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="bg" Property="Background" Value="#C50F1F"/>
                                <Setter Property="Foreground" Value="#FFFFFF"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="StatLabel" TargetType="TextBlock">
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Foreground" Value="{DynamicResource ThemeSettingsDescFg}"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>

        <Style x:Key="StatValue" TargetType="TextBlock">
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="{DynamicResource ThemeSettingsLabelFg}"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="HorizontalAlignment" Value="Right"/>
        </Style>

        <Style x:Key="OkButton" TargetType="Button">
            <Setter Property="Background" Value="{DynamicResource ThemeSettingsAccent}"/>
            <Setter Property="Foreground" Value="#FFFFFF"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="3">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Opacity" Value="0.85"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Opacity" Value="0.7"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border Background="{DynamicResource ThemeSettingsContentBg}"
            BorderBrush="{DynamicResource ThemeSettingsDivider}"
            BorderThickness="1"
            CornerRadius="4">
        <DockPanel>
            <!-- Stats grid -->
            <StackPanel Margin="20,16,20,20">

                <Grid Margin="0,0,0,14">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="28"/>
                        <RowDefinition Height="28"/>
                        <RowDefinition Height="28"/>
                        <RowDefinition Height="1"/>
                        <RowDefinition Height="28"/>
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" Grid.Column="0" Style="{StaticResource StatLabel}" Text="Characters"/>
                    <TextBlock Grid.Row="0" Grid.Column="1" Style="{StaticResource StatValue}" x:Name="CharCountText"/>

                    <TextBlock Grid.Row="1" Grid.Column="0" Style="{StaticResource StatLabel}" Text="Words"/>
                    <TextBlock Grid.Row="1" Grid.Column="1" Style="{StaticResource StatValue}" x:Name="WordCountText"/>

                    <TextBlock Grid.Row="2" Grid.Column="0" Style="{StaticResource StatLabel}" Text="Lines"/>
                    <TextBlock Grid.Row="2" Grid.Column="1" Style="{StaticResource StatValue}" x:Name="LineCountText"/>

                    <!-- Divider -->
                    <Rectangle Grid.Row="3" Grid.Column="0" Grid.ColumnSpan="2"
                               Fill="{DynamicResource ThemeSettingsDivider}"
                               Margin="0,4"/>

                    <TextBlock Grid.Row="4" Grid.Column="0" Style="{StaticResource StatLabel}" Text="Tokenized bytes"/>
                    <TextBlock Grid.Row="4" Grid.Column="1" Style="{StaticResource StatValue}" x:Name="TokenBytesText"/>
                </Grid>

                <TextBlock FontSize="11"
                           Foreground="{DynamicResource ThemeSettingsDescFg}"
                           TextWrapping="Wrap"
                           Margin="0,0,0,16"
                           Text="Tokenized bytes is the C64 BASIC memory footprint (of 38,911 bytes available)."/>

                <Button Style="{StaticResource OkButton}"
                        Content="OK" Width="90" Height="30"
                        HorizontalAlignment="Center"
                        IsDefault="True"
                        Click="CloseButton_Click"/>
            </StackPanel>

        </DockPanel>
    </Border>

</Window>
